# Configuration file for "gbp <command>"

[DEFAULT]
# the default build command:
builder = sbuild -A -v -d unstable
#builder = git-pbuilder -i -I -us -uc
# use pristine-tar:
#pristine-tar = True
# user color when on a terminal, alternatives: on/true, off/false or auto
color = auto
